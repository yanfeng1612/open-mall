spring:
  application:
    name: open-mall-user

  profiles:
    active: @boot.profile@

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.openmall.user.domain

spring.resources.static-locations: classpath:static/,file:static/
