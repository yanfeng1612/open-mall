spring:
  application:
    name: open-mall-product

  profiles:
    active: @boot.profile@

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.openmall.product.domain

spring.resources.static-locations: classpath:static/,file:static/
