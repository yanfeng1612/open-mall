server:
  port: 9002

eureka:
  client:
    service-url:
      defaultZone:  http://registry.open-mall.com:8761/eureka/

  instance:
    prefer-ip-address: true

spring:
  datasource:
    druid:
      url: jdbc:mysql://192.168.0.107:3306/open_mall_product?serverTimezone=GMT%2B8&characterEncoding=utf-8&useUnicode=true&autoReconnect=true&connectTimeout=30000&initialTimeout=1&enableQueryTimeouts=true&socketTimeout=2000&allowMultiQueries=true
      username: root
      password: 111111
      initial-size: 10
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.jdbc.Driver
